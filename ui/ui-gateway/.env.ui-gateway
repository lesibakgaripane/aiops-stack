# AIOps UI Gateway environment configuration (lab/dev)

# JWT signing configuration
JWT_SECRET_KEY=aiops_dev_secret_2025_01
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Admin credentials (lab defaults)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=password

# Service ports (keep aligned with your ecosystem)
UI_GATEWAY_PORT=8089
AI_ORCHESTRATOR_PORT=9088
FASTAPI_HEARTBEAT_PORT=8080
AIOPS_RAG_PORT=8000
AIOPS_ANOMALY_PORT=8100
